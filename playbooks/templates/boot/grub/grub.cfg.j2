set default=0
set timeout=0

menuentry "VyOS AMI (HVM) {{ version_string }}" {
  linux /boot/{{ version_string }}/vmlinuz boot=live quiet vyos-union=/boot/{{ version_string }} console=ttyS0
  initrd /boot/{{ version_string }}/initrd.img
}
